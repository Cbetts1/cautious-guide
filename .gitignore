# Python cache
__pycache__/
*.py[cod]
*.pyo

# AIOS runtime / user data (lives in home dir, never in the project tree)
# Note: ~/.aios/ is outside the repo; this entry handles any accidental
# checkout that lands .aios_userdata/ inside the project root.
.aios_userdata/

# Virtual environments
venv/
.venv/
env/

# Editor temp files
*.swp
*.swo
*~
.DS_Store
